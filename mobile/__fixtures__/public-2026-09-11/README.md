These are the exact core bytes and manifest captured from AR-local's public
`app-payload-latest` release on 2026-09-11. They were retained in the producer's
`docs/evidence/backup-observation-20260911/publication/` evidence collection.
The manifest binds `core.json.gz` by SHA-256. Rate rows are unchanged.

Tests exercise the observed 460 zero-rate rows, 159 excluded mortgage discount
rows, and distinct source/published product populations. Accounting metadata
is constructed from these retained counts to test the additive protocol.
