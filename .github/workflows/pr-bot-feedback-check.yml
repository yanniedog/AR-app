name: pr-bot-feedback-check

# Thread closure gate. Runs on PR lifecycle AND on review-comment activity so an
# in-thread reply (or a bot posting a finding) re-evaluates the gate automatically
# - no manual re-run / empty re-trigger commit needed. Each run is single-shot:
# unresolved feedback fails promptly instead of occupying a runner in a long poll.
# Self-heal triggers are limited to events whose check-run attaches to the PR
# HEAD: pull_request (push) and pull_request_review[_comment]. Generated PR
# publishers also dispatch this workflow explicitly with --ref <PR branch>, so
# that workflow_dispatch check is attached to the generated PR head.
# GitHub emits no event when a thread is resolved, so reply-triggered runs use a
# short bounded retry to observe the immediately following resolution.
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  pull_request_review:
    types: [submitted]
  pull_request_review_comment:
    types: [created, edited, deleted]
  workflow_dispatch:
    inputs:
      pr_number:
        description: Pull request number
        required: true
        type: string
permissions:
  contents: read
  pull-requests: read

jobs:
  bot-feedback-gate:
    if: >-
      github.event.pull_request != null
      || github.event.review != null
      || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "24"
      - name: Skip thread gate for bot-authored or exact generated PRs
        id: gate_exempt
        env:
          GH_TOKEN: ${{ secrets.BOT_GATE_TOKEN || github.token }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          PR_AUTHOR_TYPE: ${{ github.event.pull_request.user.type }}
          EVENT_NAME: ${{ github.event_name }}
          INPUT_PR: ${{ inputs.pr_number }}
          EVENT_PR: ${{ github.event.pull_request.number }}
        run: |
          case "$EVENT_NAME" in
            workflow_dispatch) PR="$INPUT_PR" ;;
            *)                 PR="$EVENT_PR" ;;
          esac
          [[ "$PR" =~ ^[0-9]+$ ]] || { echo "::error::Pull request number must be numeric"; exit 1; }
          if ! PR_STATE=$(gh api "repos/$GITHUB_REPOSITORY/pulls/$PR" --jq .state); then
            echo "::error::GitHub API could not read PR state; retry this check."
            exit 2
          fi
          if [ "$PR_STATE" != "open" ]; then
            echo "gate_exempt=true" >> "$GITHUB_OUTPUT"
            echo "gate_exempt_reason=pull-request-$PR_STATE" >> "$GITHUB_OUTPUT"
            echo "PR #$PR is $PR_STATE - stale review event skipped"
            exit 0
          fi
          export PR
          EXEMPT=$(node scripts/pr-gate-exempt-reason.mjs || true)
          if [ -n "$EXEMPT" ]; then
            echo "gate_exempt=true" >> "$GITHUB_OUTPUT"
            echo "gate_exempt_reason=$EXEMPT" >> "$GITHUB_OUTPUT"
            echo "PR #$PR is gate-exempt ($EXEMPT) - thread closure not required"
          else
            echo "gate_exempt=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Gate exempt - pass (no thread closure)
        if: steps.gate_exempt.outputs.gate_exempt == 'true'
        run: |
          echo "bot-feedback-gate: skipped (${{ steps.gate_exempt.outputs.gate_exempt_reason }} - human bot review not required)"
      - name: Review thread closure gate
        if: steps.gate_exempt.outputs.gate_exempt != 'true'
        env:
          GH_TOKEN: ${{ secrets.BOT_GATE_TOKEN || github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          EVENT_NAME: ${{ github.event_name }}
          INPUT_PR: ${{ inputs.pr_number }}
          EVENT_PR: ${{ github.event.pull_request.number }}
        run: |
          case "$EVENT_NAME" in
            workflow_dispatch) PR="$INPUT_PR" ;;
            *)                 PR="$EVENT_PR" ;;
          esac
          [[ "$PR" =~ ^[0-9]+$ ]] || { echo "::error::Pull request number must be numeric"; exit 1; }
          code=1
          for attempt in 1 2 3 4; do
            set +e
            npm run pr:bot-feedback-check -- --pr "$PR" --skip-bot-presence
            code=$?
            set -e
            if [ "$code" -eq 0 ]; then
              exit 0
            fi
            if [ "$code" -ne 1 ] || [ "$attempt" -eq 4 ]; then
              break
            fi
            echo "Feedback still open; retrying shortly in case a reply-triggered run preceded thread resolution."
            sleep 5
          done
          if [ "$code" -eq 2 ]; then
            echo "::error::GitHub API rate limit prevented the feedback audit; rerun this check."
          else
            echo "::error::Substantive review feedback remains open. Reply and resolve it; the reply event will re-run this gate."
          fi
          exit "$code"
